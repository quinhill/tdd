// insertion sort

// call first element of the array "sorted".
// look at next "unsorted" element and switch it with the "sorted" element if it's smaller. Otherwise add it to the end of the "sorted" part of the array.


const array = require('../randomArray');

// each number that gets sorted = sortedArray + 1 (i.e. include index plus 1 to sorted array)

for (let i = 0; i < array.length; i++);
  




